// Configuración específica para desarrollo local
// Este archivo se usa cuando ejecutas `npm run dev`
const config = {
  API_URL: 'http://localhost:5000',
  NODE_ENV: 'development'
};

export default config;
