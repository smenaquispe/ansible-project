---
# Ansible Collections requeridas para los playbooks
# Instalación: ansible-galaxy collection install -r requirements.yml

collections:
  # Colección para gestionar recursos de Google Cloud Platform
  - name: google.cloud
    version: ">=1.0.0"
    source: https://galaxy.ansible.com

  # Colección para gestionar recursos de Kubernetes
  - name: kubernetes.core
    version: ">=2.0.0"
    source: https://galaxy.ansible.com
